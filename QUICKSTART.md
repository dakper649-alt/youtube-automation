# 🚀 Быстрый старт - Генератор скриптов YouTube

## ✅ Готово к использованию!

Генератор скриптов теперь работает с **OpenRouter API** и использует **бесплатную модель Google Gemini Flash 1.5**!

## 📦 Установка зависимостей

```bash
pip install -r backend/requirements.txt
```

Или установите только необходимое:

```bash
pip install openai python-dotenv
```

## 🔑 Настройка API ключа

API ключ OpenRouter уже добавлен в `.env` файл:

```env
OPENROUTER_API_KEY=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
```

## 🧪 Тестирование

Запустите тестовый скрипт:

```bash
python backend/test_script_generator.py
```

Он протестирует:
1. ✅ Генерацию скрипта для YouTube видео
2. ✅ Создание промптов для изображений
3. ✅ Перевод скрипта на другой язык
4. ✅ SEO оптимизацию

## 💻 Пример использования

```python
import asyncio
import os
from dotenv import load_dotenv
from backend.services.script_gen import ScriptGenerator

async def main():
    # Загружаем API ключ
    load_dotenv()
    api_key = os.getenv('OPENROUTER_API_KEY')

    # Создаём генератор с БЕСПЛАТНОЙ моделью
    generator = ScriptGenerator(
        api_key,
        model="google/gemini-flash-1.5"
    )

    # Генерируем скрипт
    result = await generator.generate_script(
        topic="Как токсичные люди изучают ваши привычки",
        target_length=800,
        language='ru',
        style='educational',
        tone='professional'
    )

    # Выводим результат
    print(f"📝 Скрипт готов!")
    print(f"   Слов: {result['word_count']}")
    print(f"   Длительность: ~{result['estimated_duration']//60} минут")
    print(f"\n🎯 HOOK:\n{result['hook']}\n")
    print(f"📌 ЗАГОЛОВКИ:")
    for i, title in enumerate(result['title_suggestions'], 1):
        print(f"   {i}. {title}")

    # Генерируем промпты для изображений
    prompts = await generator.generate_image_prompts(
        script=result['script'],
        style="minimalist_stick_figure",
        images_per_minute=15
    )

    print(f"\n🎨 Промптов для изображений: {len(prompts)}")

asyncio.run(main())
```

## 🎨 Доступные стили изображений

- `minimalist_stick_figure` - минималистичные рисунки (по умолчанию)
- `cinematic_photography` - кинематографическая фотография
- `digital_painting` - цифровая живопись
- `cartoon` - мультяшный стиль
- `sketch` - карандашный набросок
- `vector` - векторная графика
- `3d_render` - 3D рендер
- `watercolor` - акварель
- `infographic` - инфографика
- `anime` - аниме стиль

## 🌐 Доступные модели в OpenRouter

### Бесплатные модели:
- `google/gemini-flash-1.5` ⭐ **Рекомендуется!** (быстрая и бесплатная)
- `meta-llama/llama-3.1-8b-instruct:free`
- `mistralai/mistral-7b-instruct:free`

### Платные дешёвые модели:
- `anthropic/claude-3-5-haiku-20241022` ($0.25/1M токенов)
- `openai/gpt-3.5-turbo` ($0.50/1M токенов)

Чтобы сменить модель:

```python
generator = ScriptGenerator(
    api_key,
    model="meta-llama/llama-3.1-8b-instruct:free"
)
```

## 📊 Возможности генератора

### 1. Генерация скриптов
- Разные языки (ru, en)
- Разные стили (educational, entertaining, documentary)
- Разные тона (professional, casual, humorous)
- Автоматическое создание hook, CTA и заголовков

### 2. Промпты для изображений
- Автоматическая разбивка на сцены
- Детальные промпты на английском
- 10 стилей изображений
- Настройка частоты (изображений в минуту)

### 3. Перевод скриптов
- Поддержка 10+ языков
- Сохранение стиля и тона
- Адаптация под культурный контекст

### 4. SEO оптимизация
- Интеграция ключевых слов
- Генерация SEO заголовков
- Создание описаний для YouTube
- Рекомендации тегов

## 💰 Стоимость

**БЕСПЛАТНО!** 🎉

Используется бесплатная модель Google Gemini Flash 1.5 через OpenRouter.

## 🐛 Отладка

Если что-то не работает:

1. Проверьте API ключ в `.env`
2. Убедитесь что установлены зависимости: `pip install openai python-dotenv`
3. Проверьте интернет соединение
4. Запустите тест: `python backend/test_script_generator.py`

## 📚 Документация

- [OpenRouter API](https://openrouter.ai/docs)
- [Список моделей OpenRouter](https://openrouter.ai/models)
- [Получить бесплатный API ключ](https://openrouter.ai/keys)

---

**Готово к использованию!** 🚀
