# 🎬 YouTube Automation System - Полное руководство

## 📋 Обзор системы

Полная система автоматизации создания YouTube видео от идеи до готового контента.

### Компоненты системы:

1. **ContentAnalyzer** (`backend/services/content_analyzer.py`)
   - Поиск лучших идей для видео
   - Анализ конкурентов
   - Извлечение паттернов успешного контента
   - Ранжирование идей по вирусному потенциалу
   - Анализ сезонных трендов

2. **YouTubeAutomationOrchestrator** (`backend/main_orchestrator.py`)
   - Координация всех сервисов
   - Полный пайплайн создания видео
   - Управление API ключами
   - Сохранение и загрузка проектов

3. **ScriptGenerator** (`backend/services/script_gen.py`)
   - Генерация скриптов с помощью Google Gemini
   - Создание промптов для изображений
   - Перевод на другие языки
   - SEO оптимизация

4. **YouTubeAnalyzer** (`backend/services/analyzer.py`)
   - Анализ YouTube каналов
   - Получение метрик видео
   - Поиск трендов

5. **APIKeyManager** (`backend/services/api_key_manager.py`)
   - Централизованное управление API ключами
   - Автоматическая ротация ключей
   - Статистика использования

---

## 🚀 Быстрый старт

### 1. Настройка API ключей

Создайте файл `.env` в корне проекта:

```bash
# Google Gemini API (обязательно)
GOOGLE_API_KEY_1=your_gemini_api_key_here

# YouTube Data API (опционально, для анализа конкурентов)
YOUTUBE_API_KEY_1=your_youtube_api_key_here

# Grok API (опционально, запасной вариант)
GROK_KEYS_LIST=your_grok_key_1,your_grok_key_2
```

**Где получить ключи:**
- Google Gemini: https://makersuite.google.com/app/apikey
- YouTube Data API: https://console.cloud.google.com/apis/credentials
- Grok: https://console.x.ai/

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Основные зависимости:
- `google-generativeai` - Google Gemini API
- `google-api-python-client` - YouTube Data API
- `python-dotenv` - Загрузка переменных окружения
- `openai` - Для Grok API

### 3. Запуск тестов

```bash
cd backend
python test_full_system.py
```

---

## 💡 Примеры использования

### Пример 1: Создание одного видео

```python
import asyncio
from main_orchestrator import YouTubeAutomationOrchestrator

async def create_video():
    # Инициализация
    orchestrator = YouTubeAutomationOrchestrator()

    # Создание видео
    projects = await orchestrator.create_video_pipeline(
        niche="психология",           # Ниша
        num_videos=1,                 # Количество видео
        video_length=1000,            # Длина скрипта в словах
        style='educational',          # Стиль
        tone='professional',          # Тон
        language='ru'                 # Язык
    )

    # Сохранение проекта
    orchestrator.save_project(projects[0], "my_video.json")

    print(f"✅ Видео создано: {projects[0]['idea']['title']}")

asyncio.run(create_video())
```

### Пример 2: Анализ ниши

```python
import asyncio
from main_orchestrator import YouTubeAutomationOrchestrator

async def analyze():
    orchestrator = YouTubeAutomationOrchestrator()

    # Полный анализ ниши
    report = await orchestrator.analyze_niche("productivity")

    print(f"Найдено идей: {len(report['top_ideas'])}")
    print(f"Лучшая идея: {report['recommendations']['best_idea']['title']}")
    print(f"Вирусный потенциал: {report['recommendations']['best_idea']['viral_score']}/100")

asyncio.run(analyze())
```

### Пример 3: Только поиск идей

```python
import asyncio
from services.api_key_manager import APIKeyManager
from services.content_analyzer import ContentAnalyzer

async def find_ideas():
    # Инициализация
    api_manager = APIKeyManager()
    analyzer = ContentAnalyzer(api_manager)

    # Поиск идей
    ideas = await analyzer.find_best_video_ideas(
        niche="саморазвитие",
        num_ideas=10,
        analyze_competitors=True
    )

    # Вывод топ-3
    for i, idea in enumerate(ideas[:3], 1):
        print(f"{i}. {idea['title']}")
        print(f"   Вирусный потенциал: {idea['viral_score']}/100")
        print(f"   Сложность: {idea['difficulty']}")
        print()

asyncio.run(find_ideas())
```

### Пример 4: Только генерация скрипта

```python
import asyncio
from services.api_key_manager import APIKeyManager
from services.script_gen import ScriptGenerator

async def generate_script():
    # Инициализация
    api_manager = APIKeyManager()
    gemini_key = api_manager.get_gemini_key()
    generator = ScriptGenerator(gemini_key, provider="gemini")

    # Генерация скрипта
    script = await generator.generate_script(
        topic="Как побороть прокрастинацию",
        target_length=1000,
        language='ru',
        style='educational',
        tone='professional'
    )

    print(f"Скрипт ({script['word_count']} слов, ~{script['estimated_duration']} сек):")
    print(script['script'])
    print("\nВарианты заголовков:")
    for i, title in enumerate(script['title_suggestions'], 1):
        print(f"{i}. {title}")

asyncio.run(generate_script())
```

---

## 📊 Структура проекта

```
youtube-automation/
├── backend/
│   ├── services/
│   │   ├── api_key_manager.py      # Управление API ключами
│   │   ├── content_analyzer.py     # Анализ контента и поиск идей
│   │   ├── script_gen.py           # Генерация скриптов
│   │   ├── analyzer.py             # YouTube анализатор
│   │   └── analyzer_advanced.py    # Расширенный анализатор
│   ├── main_orchestrator.py        # Главный оркестратор
│   └── test_full_system.py         # Тесты системы
├── .env                            # API ключи (НЕ коммитить!)
└── AUTOMATION_GUIDE.md             # Это руководство
```

---

## 🎯 Пайплайн создания видео

Полный пайплайн в `YouTubeAutomationOrchestrator.create_video_pipeline()`:

1. **Этап 1: Поиск идей**
   - Анализ конкурентов (если включён)
   - Извлечение паттернов успешного контента
   - Генерация идей с помощью AI
   - Ранжирование по вирусному потенциалу

2. **Этап 2: Генерация скрипта**
   - Создание структурированного скрипта
   - Hook (захватывающее начало)
   - Основная часть
   - Call-to-action (призыв к действию)
   - Варианты заголовков

3. **Этап 3: Промпты для изображений**
   - Разбиение скрипта на сцены
   - Генерация промптов для каждой сцены
   - Таймкоды для синхронизации

4. **Этап 4: Генерация изображений** (TODO)
   - Интеграция с Stable Diffusion / DALL-E
   - Создание изображений по промптам

5. **Этап 5: Создание озвучки** (TODO)
   - Интеграция с TTS сервисом
   - Генерация аудио из скрипта

6. **Этап 6: Монтаж видео** (TODO)
   - Интеграция с MoviePy / FFmpeg
   - Сборка финального видео

---

## 📈 API квота и ограничения

### Google Gemini (бесплатный tier)
- **Лимит:** 60 запросов в минуту
- **Стоимость:** Бесплатно для gemini-1.5-flash
- **Использование:** Генерация скриптов, идей, промптов

### YouTube Data API
- **Квота:** 10,000 единиц в день
- **Стоимость операций:**
  - `search.list`: 100 единиц
  - `videos.list`: 1 единица
  - `channels.list`: 1 единица
- **Рекомендация:** Используйте кэширование, анализируйте конкурентов редко

### Советы по оптимизации:
1. Отключайте анализ конкурентов если не нужен: `analyze_competitors=False`
2. Используйте `YouTubeAnalyzerAdvanced` с кэшированием
3. Ограничьте количество анализируемых видео
4. Используйте несколько API ключей для ротации

---

## 🔧 Настройка и кастомизация

### Стили скриптов

```python
# Образовательный стиль
style='educational'  # Объяснения, примеры, факты

# Развлекательный стиль
style='entertaining'  # Юмор, интрига, динамика

# Документальный стиль
style='documentary'  # Факты, истории, серьёзность
```

### Тон

```python
# Профессиональный
tone='professional'  # Серьёзный, деловой

# Разговорный
tone='casual'  # Дружелюбный, простой

# Юмористический
tone='humorous'  # С шутками, лёгкий
```

### Стили изображений

```python
image_style='minimalist_stick_figure'  # Минимализм (по умолчанию)
image_style='cinematic_photography'     # Кинематографичность
image_style='digital_painting'          # Цифровая живопись
image_style='cartoon'                   # Мультяшный стиль
image_style='anime'                     # Аниме стиль
```

---

## 🐛 Устранение неполадок

### Ошибка: "Нет доступных API ключей"
**Решение:** Проверьте `.env` файл, убедитесь что ключи указаны правильно

### Ошибка: "Quota exceeded"
**Решение:**
- Дождитесь сброса квоты (YouTube API сбрасывается в полночь по PST)
- Добавьте больше YouTube API ключей
- Используйте кэширование

### Ошибка импорта модулей
**Решение:**
```bash
export PYTHONPATH="${PYTHONPATH}:/home/user/youtube-automation/backend"
```

### Медленная генерация
**Решение:**
- Уменьшите `video_length`
- Отключите `analyze_competitors`
- Уменьшите `images_per_minute`

---

## 📚 Дополнительные ресурсы

- [Google Gemini API Docs](https://ai.google.dev/docs)
- [YouTube Data API Docs](https://developers.google.com/youtube/v3)
- [Примеры использования](./backend/test_full_system.py)

---

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь что все API ключи валидны
3. Проверьте квоту API
4. Запустите тесты: `python test_full_system.py`

---

**Создано:** 2025-12-29
**Версия:** 1.0.0
