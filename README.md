# YouTube Automation - Система автоматизации для Faceless каналов

Полностью автоматизированная система создания видео для YouTube faceless каналов с использованием AI.

## Возможности

- 🔍 **Анализ YouTube каналов** - автоматический анализ популярных каналов и трендов
- 📝 **Генерация скриптов** - создание уникальных сценариев с помощью Claude AI
- 🎨 **Генерация изображений** - создание визуального контента через Stability AI/OpenAI
- 🎤 **Озвучка** - профессиональная озвучка через ElevenLabs или Google TTS
- 🎬 **Автоматический монтаж** - сборка финального видео
- 📊 **Субтитры** - автоматическая генерация субтитров

## Структура проекта

```
youtube-automation/
├── backend/              # Серверная часть (Python)
│   ├── api/             # API endpoints
│   ├── services/        # Бизнес-логика
│   ├── models/          # Модели базы данных
│   ├── utils/           # Вспомогательные функции
│   ├── config/          # Конфигурация
│   └── main.py          # Точка входа
├── frontend/            # Веб-интерфейс (React)
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   ├── pages/       # Страницы
│   │   └── services/    # API клиенты
│   └── public/
├── projects/            # Сгенерированные видео (не в git)
└── docs/                # Документация
```

## Установка

### Требования

- Python 3.9+
- Node.js 16+
- FFmpeg (для обработки видео)

### Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd youtube-automation
```

### Шаг 2: Установка Python зависимостей

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# На Windows:
venv\Scripts\activate
# На MacOS/Linux:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 3: Установка Frontend зависимостей

```bash
cd frontend
npm install
cd ..
```

### Шаг 4: Установка FFmpeg

**Windows:**
```bash
# Используйте Chocolatey
choco install ffmpeg
```

**MacOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt-get install ffmpeg
```

## 🚀 Быстрый старт

После установки зависимостей вы можете быстро протестировать систему:

### 1. Быстрый тест (5 минут) ⚡
```bash
python quick_start.py
```
Проверит все компоненты и сгенерирует тестовый скрипт без создания полного видео.

**Что проверяется:**
- ✅ Импорты всех модулей
- ✅ API ключи (54 ElevenLabs)
- ✅ Генерация скрипта через Gemini
- ✅ Нормализация текста для TTS
- ✅ Ken Burns эффекты

### 2. Полное тестирование
```bash
python tests/run_all_tests.py
```
Запустит все юнит и интеграционные тесты системы.

### 3. Создание первого видео (40-60 минут) 🎬
```bash
python backend/create_video_cli.py
```
Запустит интерактивную CLI для создания полного видео.

**Примечание:** Для работы требуются настроенные API ключи (см. следующий раздел).

## 🔧 Тестирование API

Перед первым запуском протестируй AI API:
```bash
python backend/utils/test_apis.py
```

Это проверит работу Gemini 2.0 Flash и OpenAI GPT-4o-mini.

### Если Gemini не работает:

1. Получи OpenAI ключ: https://platform.openai.com/api-keys
2. Добавь в `.env`:
   ```
   OPENAI_API_KEY=sk-proj-your_key_here
   ```
3. Система автоматически переключится на OpenAI fallback

**Примечание:** Gemini бесплатный, OpenAI стоит ~$0.15-0.60 за 1M токенов.

## Настройка API ключей

### Шаг 1: Создайте файл .env

```bash
cp .env.example .env
```

### Шаг 2: Заполните API ключи

Откройте файл \`.env\` и добавьте свои API ключи:

#### YouTube API Key
1. Перейдите на [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект
3. Включите YouTube Data API v3
4. Создайте API ключ
5. Добавьте в \`.env\`: \`YOUTUBE_API_KEY=your_key_here\`

#### Anthropic API (Claude)
1. Зарегистрируйтесь на [Anthropic](https://www.anthropic.com/)
2. Получите API ключ
3. Добавьте в \`.env\`: \`ANTHROPIC_API_KEY=your_key_here\`

#### OpenAI API (для генерации изображений)
1. Зарегистрируйтесь на [OpenAI](https://platform.openai.com/)
2. Получите API ключ
3. Добавьте в \`.env\`: \`OPENAI_API_KEY=your_key_here\`

#### Stability AI (альтернатива для изображений)
1. Зарегистрируйтесь на [Stability AI](https://platform.stability.ai/)
2. Получите API ключ
3. Добавьте в \`.env\`: \`STABILITY_API_KEY=your_key_here\`

#### ElevenLabs (озвучка)
1. Зарегистрируйтесь на [ElevenLabs](https://elevenlabs.io/)
2. Получите API ключ
3. Добавьте в \`.env\`: \`ELEVENLABS_API_KEY=your_key_here\`

## Запуск приложения

### Запуск Backend

```bash
# Убедитесь что виртуальное окружение активировано
cd backend
python main.py
```

Backend будет доступен на \`http://localhost:8000\`

### Запуск Frontend

```bash
# В новом терминале
cd frontend
npm start
```

Frontend будет доступен на \`http://localhost:3000\`

## Использование системы

### 1. Анализ каналов
- Откройте веб-интерфейс
- Введите URL YouTube канала для анализа
- Система определит популярные темы и стиль контента

### 2. Генерация видео
- Выберите тему или позвольте AI предложить темы
- Настройте параметры (длительность, стиль, голос)
- Запустите процесс генерации

### 3. Мониторинг прогресса
- Отслеживайте статус генерации в реальном времени
- Просматривайте промежуточные результаты

### 4. Экспорт и публикация
- Скачайте готовое видео
- Используйте сгенерированные метаданные (название, описание, теги)
- Опубликуйте на YouTube

## API Endpoints

### Backend API

- \`GET /api/health\` - Проверка состояния сервера
- \`POST /api/analyze\` - Анализ YouTube канала
- \`POST /api/generate/script\` - Генерация скрипта
- \`POST /api/generate/images\` - Генерация изображений
- \`POST /api/generate/audio\` - Генерация озвучки
- \`POST /api/generate/video\` - Сборка финального видео
- \`GET /api/projects\` - Список всех проектов
- \`GET /api/projects/{id}\` - Детали проекта

## Разработка

### Структура сервисов

- **analyzer.py** - Анализ YouTube каналов, трендов, конкурентов
- **script_gen.py** - Генерация сценариев с помощью Claude AI
- **image_gen.py** - Создание визуального контента
- **tts.py** - Преобразование текста в речь
- **video_editor.py** - Автоматический монтаж видео
- **subtitle_gen.py** - Генерация субтитров

### Добавление новых функций

1. Создайте новый сервис в \`backend/services/\`
2. Добавьте API endpoint в \`backend/api/\`
3. Обновите frontend для использования нового endpoint

## Troubleshooting

### Ошибка: ModuleNotFoundError
```bash
# Убедитесь что виртуальное окружение активировано
# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: FFmpeg not found
```bash
# Убедитесь что FFmpeg установлен
ffmpeg -version
```

### Ошибка: API Key invalid
```bash
# Проверьте файл .env
# Убедитесь что все ключи корректны
cat .env
```

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

## Roadmap

- [ ] Поддержка множественных языков
- [ ] Интеграция с YouTube API для прямой публикации
- [ ] Расширенная аналитика и A/B тестирование
- [ ] Шаблоны для разных типов видео
- [ ] Планировщик публикаций

---

**Важно:** Этот проект использует AI для генерации контента. Всегда проверяйте и редактируйте сгенерированный контент перед публикацией.
